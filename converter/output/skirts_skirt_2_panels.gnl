GARMENT skirts_skirt_2_panels {

  -- Panels
  front = P(%torso.front, contour, 1.45)
  back = P(%torso.back, contour, 1.51)

  -- Build order
  BUILD:
    S(back.side_r2, front.side_l, plain)
    >>     S(back.side_r, front.side_l2, plain)
    >>     S(back.side_l2, front.side_r, plain)
    >>     S(back.side_l, front.side_r2, plain)
}
